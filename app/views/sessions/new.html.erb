<%= form_for @user, url: { action: 'create' } do |f| %>
  <%= f.collection_select :name, @users, :name, :name %>
  <%= f.label :password %>
  <%= f.text_field :password %>
  <%= f.submit "Sign In" %>
<% end %>
